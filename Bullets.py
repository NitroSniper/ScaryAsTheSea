#Why the fuck can't i draw triangles good do i have to use images...

class BulletTemplate(object):
    def update()

class SimpleBullet(BulletTemplate):
    def __init__(self, size, position, velocity, angle):
        self.size = size
        self.position = position
        self.velocity = velocity
        self.angle = angle
    def update(dt):
        pass